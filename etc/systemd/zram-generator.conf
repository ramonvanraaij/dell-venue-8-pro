# 2GB Dell Venue 8 Pro: size zram swap to full RAM (compressed, so effective swap
# is several GB with zstd). Keeps the tablet off OOM under memory pressure.
[zram0]
zram-size = ram
compression-algorithm = zstd
swap-priority = 100
