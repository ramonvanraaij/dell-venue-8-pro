[Coredump]
# Bay Trail eMMC is far too slow to process core dumps; a crash-loop turns
# coredump processing into a disk-saturating freeze. Keep crashes to a single
# journal line and never write/process a core on this device.
Storage=none
ProcessSizeMax=0
