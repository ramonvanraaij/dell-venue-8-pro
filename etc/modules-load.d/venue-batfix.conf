# Load the batfix battery transient-zero filter at boot (Dell Venue 8 Pro 5830).
# The .ko is built + installed into the kernel's updates/ dir by
# venue-batfix-build.sh (and kept current across kernel upgrades by the
# venue-batfix.hook pacman hook).
batfix
